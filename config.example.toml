[canvas]
base_url = "https://school.instructure.com"
token_env = "CANVAS_TOKEN"
# Optional for git-ignored local configs only:
# token = "paste-token-here"

[archive]
root = "~/CanvasArchive"
year = "2026"
semester = "Spring"
download_workers = 6

[google_drive]
# Optional advanced Google Drive API upload.
# Most users can leave this off and set archive.root to a local synced folder instead.
enabled = false
credentials_file = "secrets/google-client-secret.json"
token_file = "secrets/google-token.json"
root_folder_name = "Canvas Archive"
